.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Build artifacts — distributed via GitHub Releases, not source control
/dist

# Signing keys — never commit these
~/
~
*.key
*.key.pub

# Private research and strategy docs
*_elevation_analysis.md
*_go_mainstream.md
ai-researcher-report-*.md

# Browser automation session logs
.playwright-mcp/

# Scratch screenshots
devdock-ui-check.png
soryq-current.png

# AI / IDE tool local configs — machine-specific, never commit
.claude/
.kilo/
.antigravitycli/

# Cargo dev logs
src-tauri/cargo_check.log
src-tauri/cargo_output*.txt

# Per-project local data (daily notes, tasks) written by the app at runtime
.soryq/
